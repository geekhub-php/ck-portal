file_upload_image:
    pattern:  /upload-image
    defaults: { _controller: "GeekhubFileBundle:Default:uploadImage" }

file_upload_document:
    pattern:  /upload-document
    defaults: { _controller: "GeekhubFileBundle:Default:uploadDocument" }