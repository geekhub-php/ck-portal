{% extends "::base.html.twig" %}
{% block body %}
<h1>Черкаські мрії</h1>

    {% for dream in dreams %}
        <div class="front-dream">
            <a href="{{ path('dream_show', { 'slug': dream.slug }) }}"><h3>{{ dream.title }}</h3></a>
            Додати до обраного.
            <img src="{{ dream.owner.profilePicture | imagine_filter('profile_avatar_ico') }}" />{{ dream.owner.name }}
            <img src="" class="dream-main-image" />
            <div class="like">
                Підтримали: {{ dream.contribution | length }} Рейтинг: {{ dream.like }}
            </div>
            <div>
                Зібрано:
            </div>
            <div>
                Теги:<br />
                {% for tag in dream.tags %}{{ tag.name }} {% endfor %}
            </div>
        </div>
    {% endfor %}

<ul>
    <li>
        <a href="{{ path('dream_new') }}">
            Створити нову мрію
        </a>
    </li>
</ul>
{% endblock %}